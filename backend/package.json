{
  "name": "facultad-hub-backend",
  "version": "0.1.0",
  "description": "API del hub del vault de Facultad",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
